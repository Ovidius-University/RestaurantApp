@model ShortAllergenVm

@{
    ViewData["Title"] = "Delete";
}

<h1>Are you sure you want to delete the allergen: <span class="text-danger">@Html.DisplayFor(model => model.Name)</span>?</h1>
<div>
    <form asp-action="Delete">
        <input type="hidden" asp-for="AllergenId" />
        <input type="submit" value="Delete" class="btn btn-danger" />
    </form>
    <partial name="_BackToList" />
</div>
