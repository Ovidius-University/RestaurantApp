﻿﻿﻿@using WebAppUI.Models.ViewModels
@model CardIngredientVm
<div class="card">
    <div class="card-body">
        <h2 class="card-subtitle mb-2 text-body-secondary">@Html.DisplayFor(m => Model.Name)</h2>
        <hr />
        @*<a asp-action="Details" asp-route-id="@Model.IngredientId" class="card-link">Foods</a>*@
        @Html.ActionLink("Food items", "Details", "Ingredient", new { id = Model.IngredientId }, new { @class = "card-link" })
    </div>
</div>