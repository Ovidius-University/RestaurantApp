@model IEnumerable<WebAppUI.Models.ViewModels.CardChefVm>
<article class="me-2">
    <h1>Chefs</h1>
</article>
<section class="row">
    @if (Model.Count() > 0)
    {
        @foreach (var item in Model)
        {
            <article class="me-2 col-lg-3">
                @await Html.PartialAsync("Chef/_Card", item)
            </article>
        }
    }
    else
    {
        <article class="alert alert-warning" role="alert">There is no registered chef yet!</article>
    }
</section>